name: Label Checker

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
      - unlabeled

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  Check_SemVer_Labels:
    name: Check Semver labels
    runs-on: ubuntu-latest
    steps:

      - name: Check for Semver labels
        uses: danielchabr/pr-labels-checker@v3.1
        with:
          hasSome: major,minor,patch
          githubToken: ${{ secrets.GITHUB_TOKEN }}

  Check_CC_Labels:
    name: Check conventional commits labels
    runs-on: ubuntu-latest
    steps:

      - uses: danielchabr/pr-labels-checker@v3.1
        with:
          hasSome: enhancement,feature,fix,bug,changed,deprecated,removed,security,docs,dependencies
          githubToken: ${{ secrets.GITHUB_TOKEN }}