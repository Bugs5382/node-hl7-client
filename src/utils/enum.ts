/** @internal */
export enum Delimiters {
  Segment,
  Field,
  Component,
  Repetition,
  Escape,
  SubComponent
}

/** @internal */
export enum READY_STATE {
  CONNECTING,
  CONNECTED,
  OPEN,
  CLOSING,
  CLOSED
}
